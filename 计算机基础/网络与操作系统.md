# 网络 & 操作系统

## 一、网络分层模型

### 1. OSI 七层模型 vs TCP/IP 四层模型

```
OSI 七层模型            TCP/IP 四层模型         五层模型（教学常用）
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  7. 应用层    │      │              │      │              │
├──────────────┤      │   应用层      │      │   应用层      │
│  6. 表示层    │      │ (HTTP/DNS/   │      │              │
├──────────────┤      │  FTP/SMTP)   │      │              │
│  5. 会话层    │      │              │      │              │
├──────────────┤      ├──────────────┤      ├──────────────┤
│  4. 传输层    │      │   传输层      │      │   传输层      │
│              │      │  (TCP/UDP)   │      │              │
├──────────────┤      ├──────────────┤      ├──────────────┤
│  3. 网络层    │      │   网际层      │      │   网络层      │
│              │      │  (IP/ICMP)   │      │              │
├──────────────┤      ├──────────────┤      ├──────────────┤
│  2. 数据链路层│      │  网络接口层   │      │  数据链路层   │
├──────────────┤      │  (Ethernet/  │      ├──────────────┤
│  1. 物理层    │      │   WiFi/PPP)  │      │   物理层      │
└──────────────┘      └──────────────┘      └──────────────┘
```

### 2. 各层详解

| 层 | OSI 层名 | 核心功能 | 典型协议/技术 | 数据单元 | 典型设备 |
|---|---------|---------|-------------|---------|--------|
| 7 | 应用层 | 为应用程序提供网络服务接口 | HTTP、HTTPS、DNS、FTP、SMTP、SSH、WebSocket | 报文（Message）| 网关、WAF |
| 6 | 表示层 | 数据格式转换、加密/解密、压缩 | SSL/TLS、JPEG、ASCII、JSON | 报文 | — |
| 5 | 会话层 | 建立/管理/终止会话，会话恢复 | RPC、NetBIOS、Socket（抽象）| 报文 | — |
| 4 | 传输层 | 端到端可靠传输、流量控制、拥塞控制 | **TCP**、**UDP**、QUIC | 段（Segment）/ 数据报（Datagram）| — |
| 3 | 网络层 | 逻辑寻址（IP）、路由选择、分组转发 | **IP**、ICMP、ARP、OSPF、BGP | 包（Packet）| **路由器** |
| 2 | 数据链路层 | 帧封装、MAC 寻址、差错检测、介质访问控制 | Ethernet、WiFi（802.11）、PPP、VLAN | 帧（Frame）| **交换机**、网桥 |
| 1 | 物理层 | 比特流传输、电气/光学信号转换 | RJ45、光纤、无线电波、USB | 比特（Bit）| 集线器、中继器、网线 |

> **面试要点**：TCP/IP 实际只有四层，将 OSI 的应用层+表示层+会话层合并为应用层，将数据链路层+物理层合并为网络接口层。日常开发和面试中**五层模型**（应用层/传输层/网络层/数据链路层/物理层）最常用。

### 3. 数据封装与解封装

```
发送方（自上而下封装）                              接收方（自下而上解封装）

应用层     │ 原始数据（HTTP请求）│                    │ 原始数据 │  ← 应用层
           ↓ + TCP头                                  ↑ 去掉TCP头
传输层     │ TCP头 │ 数据 │                            │ TCP头 │ 数据 │  ← 传输层
           ↓ + IP头                                    ↑ 去掉IP头
网络层     │ IP头 │ TCP头 │ 数据 │                     │ IP头 │ TCP头 │ 数据 │  ← 网络层
           ↓ + MAC头 + FCS                              ↑ 去掉MAC头和FCS
数据链路层 │ MAC头 │ IP头 │ TCP头 │ 数据 │ FCS │        │ MAC头 │ ... │ FCS │  ← 数据链路层
           ↓ 转为比特流                                  ↑ 比特流还原为帧
物理层     │ 01010110110... │                           │ 01010110110... │  ← 物理层
```

- **封装**：每一层在上层数据前面加上自己的头部（Header），形成该层的协议数据单元（PDU）
- **解封装**：接收方每一层去掉本层头部，把有效载荷交给上层处理
- **关键理解**：每层只关心对等层的协议交互，不关心其他层的实现细节（**分层解耦**）

### 4. 一次 HTTP 请求的完整网络过程

```
浏览器输入 http://www.example.com/api

① 应用层：构造 HTTP 请求报文（GET /api HTTP/1.1 ...）
② DNS 解析：www.example.com → 93.184.216.34（递归查询→根→顶级→权威）
③ 传输层：TCP 三次握手建立连接（SYN→SYN+ACK→ACK），分配端口号
④ 网络层：源IP→目标IP，路由器逐跳转发（查路由表，TTL 递减）
⑤ 数据链路层：ARP 解析下一跳 MAC 地址，封装以太网帧
⑥ 物理层：转为电信号/光信号在介质上传输
⑦ 到达目标服务器后逐层解封装，Web 服务器处理请求
⑧ 响应数据按相同路径反向封装、传输、解封装
⑨ 浏览器收到响应，渲染页面
```

### 5. 核心协议速览

#### 5.1 ARP（地址解析协议）— 网络层/链路层之间

```
目的：已知目标 IP，解析目标 MAC 地址（同一局域网内）

流程：
1. 主机 A 查 ARP 缓存表 → 没有 B 的 MAC
2. 主机 A 广播 ARP 请求："谁是 192.168.1.2？请告诉 192.168.1.1"
3. 主机 B 收到请求，单播回复："192.168.1.2 的 MAC 是 AA:BB:CC:DD:EE:FF"
4. 主机 A 缓存 IP→MAC 映射（有过期时间，默认 20 分钟）

跨网段通信：ARP 只解析网关的 MAC，数据发给网关，由路由器转发
```

#### 5.2 DNS（域名系统）— 应用层

```
域名解析流程（递归 + 迭代）：

浏览器 DNS 缓存 → OS DNS 缓存 → hosts 文件
        ↓ 未命中
本地 DNS 服务器（递归查询）
        ↓ 未命中，开始迭代查询
根域名服务器（.）  →  返回 .com 顶级域 NS
        ↓
顶级域服务器（.com） →  返回 example.com 权威 NS
        ↓
权威域名服务器（example.com）→  返回 A 记录 93.184.216.34
        ↓
本地 DNS 缓存结果（TTL 时间内有效），返回给客户端
```

**常见 DNS 记录类型：**
| 记录类型 | 含义 | 示例 |
|---------|------|------|
| A | 域名 → IPv4 | `example.com → 93.184.216.34` |
| AAAA | 域名 → IPv6 | `example.com → 2606:2800:...` |
| CNAME | 域名别名 | `www.example.com → example.com` |
| MX | 邮件服务器 | `example.com → mail.example.com` |
| NS | 域名的权威DNS服务器 | `example.com → ns1.example.com` |
| TXT | 文本记录（SPF/DKIM验证）| `v=spf1 include:...` |

#### 5.3 ICMP（Internet 控制报文协议）— 网络层

- **ping**：发送 ICMP Echo Request，收到 Echo Reply 表示可达
- **traceroute**：利用逐步增大 TTL 值，每跳路由器返回 ICMP Time Exceeded，追踪路由路径
- ICMP 差错报告：目标不可达、重定向、超时等

### 6. 面试标准答法

> 网络分层模型主要有两个：**OSI 七层**和 **TCP/IP 四层**。实际使用最广的是 TCP/IP 模型，自上而下分为：**应用层**（HTTP/DNS 等协议，直接面向用户）、**传输层**（TCP 提供可靠传输，UDP 提供轻量传输）、**网络层**（IP 负责逻辑寻址和路由转发）、**网络接口层**（以太网帧封装、MAC 寻址、物理传输）。分层的核心思想是**解耦**：每层只负责一件事，通过定义好的接口和协议与相邻层交互，上层不关心下层实现细节。数据发送时自上而下逐层封装头部，接收时自下而上逐层解封，对等层之间逻辑通信。

### 7. 常见面试追问

**Q: 为什么要分层？不分层行不行？**
> 不分层技术上也能实现网络通信（早期确实有不分层的协议），但分层有三大好处：① **解耦**：各层独立演进，如传输层从 TCP 换成 QUIC 不影响应用层；② **复用**：同一层协议可被多个上层使用（TCP 既可承载 HTTP 也可承载 FTP）；③ **标准化**：不同厂商只要实现同一层的协议标准就能互通。代价是多层封装带来一定的头部开销和性能损耗。

**Q: TCP/IP 模型和 OSI 模型的区别？**
> OSI 是国际标准化组织、先有理论模型后有实现，分为 7 层，概念清晰但过于复杂（表示层/会话层实际很少独立实现）。TCP/IP 是先有实现再总结模型，分为 4 层，更贴近实际。两者核心差异就在上三层的处理：OSI 严格区分应用/表示/会话三层功能，TCP/IP 将它们合并为一个应用层。面试和实际开发中多用五层模型（TCP/IP 的网络接口层拆分为数据链路层+物理层），兼顾理论清晰和实际贴合。

**Q: 浏览器输入 URL 到页面展示经历了什么？**
> ① **DNS 解析**（浏览器缓存→OS 缓存→本地 DNS→根→顶级→权威）；② **TCP 三次握手**建立连接；③ 如果是 HTTPS 则进行 **TLS 握手**（证书验证+密钥协商）；④ 发送 **HTTP 请求**（方法/路径/头部/Cookie）；⑤ 服务端处理请求，返回 **HTTP 响应**（状态码/响应头/HTML）；⑥ 浏览器**解析 HTML**（DOM 树→CSSOM→渲染树→布局→绘制），遇到 JS/CSS/图片再发请求；⑦ **TCP 四次挥手**关闭连接（Keep-Alive 可复用）。

**Q: 同一局域网内两台主机通信经过哪些层？**
> 只经过**应用层→传输层→网络层→数据链路层→物理层**，不需要经过路由器（三层设备），在数据链路层通过 **ARP 协议**解析目标 MAC 地址后，由交换机（二层设备）直接转发以太网帧到目标主机。

---

## 二、TCP 三次握手 & 四次挥手

### 1. 三次握手

```
客户端（Client）                     服务端（Server）
    │                                     │
    │  ① SYN（seq=x）                     │  LISTEN 状态
    │ ─────────────────────────────────→  │
    │                                     │  SYN_RCVD 状态
    │  ② SYN+ACK（seq=y, ack=x+1）        │
    │ ←─────────────────────────────────  │
    │  ESTABLISHED 状态                   │
    │  ③ ACK（seq=x+1, ack=y+1）          │
    │ ─────────────────────────────────→  │
    │                                     │  ESTABLISHED 状态
```

**为什么是三次而不是两次？**
- 两次握手只能保证客户端→服务端的单向信道可用
- 第三次 ACK 让服务端确认"客户端能收到服务端的数据"，完成双向信道验证
- 防止历史失效连接请求（网络延迟的旧 SYN）被服务端误当成新连接建立

**SYN Flood 攻击：** 攻击者发大量 SYN 不回应 ACK，服务端维护大量半开连接（SYN_RCVD），耗尽连接资源。
对策：`SYN Cookie`（服务端不存 SYN，将状态编码进 seq=y 返回，只有合法 ACK 才能重建状态）。

### 2. 四次挥手

```
主动关闭方（Client）                  被动关闭方（Server）
    │                                     │
    │  ① FIN（seq=u）                     │
    │ ─────────────────────────────────→  │
    │  FIN_WAIT_1                         │  CLOSE_WAIT
    │                                     │  （可继续发数据，半关闭状态）
    │  ② ACK（ack=u+1）                   │
    │ ←─────────────────────────────────  │
    │  FIN_WAIT_2                         │
    │                                     │  （Server 数据发完...）
    │  ③ FIN（seq=v）                     │
    │ ←─────────────────────────────────  │
    │                                     │  LAST_ACK
    │  ④ ACK（ack=v+1）                   │
    │ ─────────────────────────────────→  │
    │  TIME_WAIT（等待 2MSL）              │  CLOSED
    │  → CLOSED                           │
```

**为什么挥手需要四次而握手只需三次？**
- 握手时 SYN+ACK 可以合并（服务端同时确认+发起）
- 挥手时服务端收到 FIN 后，可能还有数据未发完（半关闭状态），ACK 和 FIN 必须分开发

**TIME_WAIT 为什么等待 2MSL（2 × 最大报文生存时间，约 60s）？**
1. 确保最后一个 ACK 能到达服务端（ACK 丢失时服务端会重发 FIN，需要时间重传）
2. 让网络中残留的旧连接报文全部消散，防止新连接收到旧连接的数据

**TIME_WAIT 过多的问题：**
- 大量短连接场景（如 HTTP/1.0 每次请求都新建连接）会堆积大量 TIME_WAIT，占用端口资源
- 解决：① 开启 `SO_REUSEADDR` 允许复用 TIME_WAIT 端口；② HTTP 长连接 Keep-Alive；③ `tcp_tw_reuse`（复用 TIME_WAIT 连接，仅客户端有效）

### 3. 常见面试追问

**Q: 为什么 TCP 建连需要三次，断连需要四次？**
> 建连时服务端可以把 SYN 和 ACK 合并成一个报文（同时表示"我收到了"和"我也想建连"）；断连时，服务端收到 FIN 只代表客户端不再发数据，但服务端可能仍有数据要发（半关闭状态），所以先单独 ACK 确认，等服务端数据发完再单独发 FIN。本质原因：TCP 是全双工，断连需要双方各自独立关闭自己的发送方向。

**Q: 如果第三次握手的 ACK 丢失了怎么办？**
> 服务端停留在 SYN_RCVD 状态，超时后会重发 SYN+ACK（默认重试 5 次，间隔指数退避）。客户端已经是 ESTABLISHED，收到重发的 SYN+ACK 后会再次发 ACK。若重试次数耗尽，服务端关闭该半开连接。

---

## 三、TCP 可靠性保证

### 1. 可靠性机制全景

```
┌─────────────────────────────────────────────────────────────┐
│                   TCP 可靠性机制                             │
├─────────────────┬───────────────────────────────────────────┤
│  序列号 & ACK    │ 每个字节都有序列号，接收方 ACK 确认已收到  │
│  超时重传        │ 未收到 ACK → RTO 超时 → 重发             │
│  快速重传        │ 收到 3 个重复 ACK → 立即重发（不等超时）  │
│  流量控制        │ 接收窗口（rwnd）限制发送速率，防止接收方   │
│                 │ 缓冲区溢出                                │
│  拥塞控制        │ 拥塞窗口（cwnd）根据网络状态动态调整      │
│  校验和          │ 检测数据传输中的错误（位翻转等）           │
│  连接管理        │ 三次握手/四次挥手保证双向通道可靠建立     │
└─────────────────┴───────────────────────────────────────────┘
```

### 2. 滑动窗口

```
发送方维护发送窗口（min(cwnd, rwnd)）：

已发已确认 │ 已发未确认 │ 可发未发 │ 不可发
─────────┼───────────┼─────────┼──────────
         ← 发送窗口 →
         
窗口滑动：收到 ACK 后，窗口向右移动
```

**零窗口问题：** 接收方 rwnd=0 时，发送方停止发送，开启 Persist Timer 定时发探测报文，等待窗口恢复。

### 3. 拥塞控制四阶段

```
cwnd
  │                  指数增长（慢启动）
  │               /
ssthresh──────────           线性增长（拥塞避免）
  │             /──────────────────
  │            /                    \  发生丢包
  │           /                      \（丢包 → ssthresh=cwnd/2，cwnd=1 重来）
  │          /
  │─────────
  1
  └──────────────────────────────────────── RTT

① 慢启动：cwnd < ssthresh，每个 RTT 翻倍（指数增长）
② 拥塞避免：cwnd ≥ ssthresh，每个 RTT 加 1（线性增长）
③ 快速重传：3 个重复 ACK → ssthresh = cwnd/2，cwnd = ssthresh（从中间重来，比从 1 开始快）
④ 快速恢复：配合快速重传，不从 1 开始，直接从新 ssthresh 开始拥塞避免
```

### 4. 面试标准答法

> TCP 可靠性靠五个机制协同保证：① **序列号+ACK**：对每个字节编号，接收方 ACK 确认，发送方知道哪些已收到；② **超时重传**：RTO 内未收到 ACK 则重传，RTO 基于 RTT 动态计算（Karn/Jacobson 算法）；③ **快速重传**：收到 3 个重复 ACK 意味着一个包丢了但后续包到了，不等超时立即重传；④ **流量控制**：接收方通过 TCP header 中的窗口字段（rwnd）告知发送方自己还能接收多少，防止缓冲区溢出；⑤ **拥塞控制**（慢启动+拥塞避免+快速恢复）动态探测网络容量上限，防止网络拥塞。

### 5. 常见追问

**Q: TCP 和 UDP 的区别，什么场景用 UDP？**
> TCP：面向连接、可靠、有序、流量/拥塞控制、较大头部开销（20B）。UDP：无连接、不可靠、无序、无拥塞控制、头部 8B。选 UDP 的场景：① 实时性优先于可靠性（音视频通话，丢帧可接受，重传反而增加延迟）；② 广播/多播（TCP 不支持）；③ DNS 查询（一问一答，短小，可自行实现重试）；④ 游戏信令（有些游戏自建可靠层在 UDP 上，KCP 协议）。QUIC（HTTP/3 底层）也是基于 UDP 自建可靠传输。

**Q: TCP 粘包 / 拆包是什么，如何解决？**
> TCP 是字节流协议，没有消息边界概念，发送的多条消息可能被合并（粘包）或拆分（拆包）。解决方案：① **固定长度**：每条消息固定字节数，满了就是一条；② **分隔符**：特殊字符（如 `\n`）分隔，接收方按分隔符切割；③ **长度前缀**：消息头 4 字节表示消息体长度，先读 4 字节，再读对应长度（最常用，Netty/PB/Dubbo 都是此方案）。

---

## 四、HTTP vs HTTPS

### 1. 核心区别

| | HTTP | HTTPS |
|---|---|---|
| 默认端口 | 80 | 443 |
| 传输层 | 明文 TCP | SSL/TLS 加密 |
| 安全性 | 可被中间人截获 | 加密 + 身份验证 |
| 速度 | 快 | 略慢（TLS 握手 + 加解密开销）|
| 证书 | 不需要 | 需要 CA 颁发的数字证书 |

### 2. TLS 握手过程（TLS 1.2）

```
Client                                Server
  │                                     │
  │  ① ClientHello                      │
  │  （TLS版本、随机数1、支持的加密套件）  │
  │ ─────────────────────────────────→  │
  │                                     │
  │  ② ServerHello + Certificate        │
  │  （选定加密套件、随机数2、服务端证书） │
  │ ←─────────────────────────────────  │
  │                                     │
  │  ③ 客户端验证证书（CA链验证）         │
  │    生成 Pre-Master-Secret（随机数3） │
  │    用服务端公钥加密，发送给服务端      │
  │  ClientKeyExchange                  │
  │ ─────────────────────────────────→  │
  │                                     │
  │  双方各自用随机数1+2+3 推导出         │
  │  对称加密 Session Key               │
  │                                     │
  │  ④ ChangeCipherSpec（准备切换加密）   │
  │  ⑤ Finished（握手摘要，验证一致性）  │
  │ ←─────────────→                     │
  │                                     │
  │  ═══ 后续用 Session Key 对称加密 ═══ │
```

**TLS 1.3 改进：** 1-RTT 握手（减少一次往返），0-RTT 会话复用（再次连接直接发数据），废弃 RSA 密钥交换（改用 ECDHE，保证前向安全）。

### 3. 证书验证（防中间人攻击）

```
CA（根证书颁发机构）
  └── 中间 CA（颁发的中间证书）
        └── 服务端证书（包含域名、公钥、CA签名）

验证流程：
1. 客户端收到服务端证书
2. 用中间 CA 公钥验证服务端证书签名 → 合法
3. 用根 CA 公钥验证中间 CA 证书签名 → 合法
4. 根证书预装在操作系统/浏览器中 → 信任锚点
5. 验证域名匹配（CN/SAN字段）
6. 验证证书未过期、未被吊销（OCSP/CRL）
```

### 4. HTTP/1.1 → HTTP/2 → HTTP/3

| | HTTP/1.1 | HTTP/2 | HTTP/3 |
|---|---|---|---|
| **底层协议** | TCP | TCP | UDP（QUIC）|
| **多路复用** | 无（队头阻塞）| ✅ 二进制帧+流 | ✅ QUIC 流独立 |
| **头部压缩** | 无 | ✅ HPACK | ✅ QPACK |
| **服务端主动推送** | ❌ | ✅ Server Push | ✅ |
| **队头阻塞** | 应用层 | TCP 层仍有 | ✅ 消除（UDP）|
| **握手延迟** | 1-RTT TCP | 1-RTT TCP | 0-RTT |

### 5. 面试标准答法

> HTTP 和 HTTPS 的核心区别是 HTTPS 在 TCP 之上加了 TLS 层，实现了三件事：① **加密**（防窃听，对称加密传输数据，非对称加密交换密钥）；② **身份验证**（CA 证书机制，防中间人伪装服务端）；③ **完整性校验**（MAC，防数据篡改）。TLS 握手在 TCP 三次握手后进行，TLS 1.2 需要额外 2-RTT，TLS 1.3 优化到 1-RTT，HTTPS 延迟已经可以接受。

### 6. 常见追问

**Q: HTTPS 能防止所有中间人攻击吗？**
> 不能完全防止。以下情况仍有风险：① **证书信任问题**：客户端被植入伪造的根证书（企业/恶意软件），中间人可以用该根 CA 签发任意域名的证书；② **证书吊销未及时检查**：OCSP/CRL 有缓存，吊销不立即生效（OCSP Stapling 可缓解）；③ **SSLstrip 降级攻击**：将 HTTPS 降级为 HTTP（HSTS 头解决，`Strict-Transport-Security` 强制 HTTPS）。

**Q: HTTPS 的对称密钥是怎么协商出来的？**
> 以 TLS 1.2 RSA 为例：客户端生成随机 Pre-Master-Secret，用服务端公钥（证书中）加密发给服务端，服务端用私钥解密。双方各有：随机数1（Client Random）、随机数2（Server Random）、Pre-Master-Secret，通过 PRF（伪随机函数）推导出 Session Key（会话密钥），后续用 AES 等对称加密通信。TLS 1.3 改用 ECDHE：双方各生成临时公私钥对，交换公钥，独立推导出共享密钥（Diffie-Hellman），不传输任何密钥本身，即使私钥泄露也不影响历史会话（前向安全）。

---

## 五、零拷贝原理

### 1. 传统 IO 数据拷贝（4 次拷贝 + 4 次上下文切换）

```
应用程序读取文件并通过网络发送（read + write）：

磁盘 ──DMA拷贝──→ 内核 PageCache ──CPU拷贝──→ 用户缓冲区（read 返回）
                                                    │
           ──────────────────────────── CPU拷贝 ←──┘
           ↓
     内核 Socket 发送缓冲区 ──DMA拷贝──→ 网卡（发送）

切换：① 用户→内核（read）② 内核→用户（read 返回）③ 用户→内核（write）④ 内核→用户（write 返回）
```

### 2. sendfile 零拷贝（2 次拷贝 + 2 次上下文切换）

```java
// Linux sendfile 系统调用
// Java 中 FileChannel.transferTo() 底层使用 sendfile

FileChannel fileChannel = new FileInputStream("file.txt").getChannel();
SocketChannel socketChannel = serverSocket.accept();
// 数据不经过用户态，直接从 PageCache 传到 Socket 缓冲区
fileChannel.transferTo(0, fileChannel.size(), socketChannel);
```

```
磁盘 ──DMA拷贝──→ 内核 PageCache ──CPU拷贝──→ 内核 Socket 缓冲区 ──DMA拷贝──→ 网卡

切换：① 用户→内核（sendfile）② 内核→用户（sendfile 返回）
```

### 3. sendfile + DMA Scatter-Gather（真正的零 CPU 拷贝）

Linux 2.4+ 支持 DMA Scatter-Gather，连 CPU 拷贝也省掉：

```
磁盘 ──DMA拷贝──→ 内核 PageCache
                      │ 仅传文件描述符（fd + offset + length）给 Socket 缓冲区
                      ↓
              Socket 缓冲区（仅有 fd 信息，不复制数据）
                      │
                      ↓ DMA Scatter-Gather 协议
                    网卡直接从 PageCache 读

总拷贝：2 次 DMA（无 CPU 拷贝）
```

### 4. mmap + write（3 次拷贝）

```java
MappedByteBuffer buffer = fileChannel.map(FileChannel.MapMode.READ_ONLY, 0, size);
// buffer 是 PageCache 的用户态映射（不复制数据，只映射地址）
socketChannel.write(buffer);  // 用户态直接操作内核 PageCache
```

```
磁盘 ──DMA拷贝──→ 内核 PageCache
                  ↑ mmap 直接映射（无拷贝）
             用户态虚拟地址（buffer）
                  │ write 时
                  ↓ CPU拷贝
             Socket 发送缓冲区 ──DMA拷贝──→ 网卡
```

### 5. 各方案对比

| 方案 | CPU 拷贝 | DMA 拷贝 | 上下文切换 | 适用场景 |
|---|---|---|---|---|
| 传统 read+write | 2 | 2 | 4 | 通用 |
| mmap+write | 1 | 2 | 4 | 文件内容需修改 |
| sendfile | 1 | 2 | 2 | **文件直接发网络（Kafka、Nginx）**|
| sendfile+DMA SG | 0 | 2 | 2 | 大文件高吞吐（需网卡支持）|

**Kafka 使用零拷贝：** Consumer 拉取消息时，Broker 用 `FileChannel.transferTo()` 将日志文件直接发给 Socket，无需经过用户态，吞吐极高。

### 6. 面试标准答法

> 零拷贝是指减少数据在内存中的冗余复制，降低 CPU 干预。传统 IO 读文件发网络需要 4 次内存拷贝（磁盘→PageCache，PageCache→用户缓冲区，用户缓冲区→Socket 缓冲区，Socket 缓冲区→网卡）+ 4 次用户态/内核态切换。`sendfile` 系统调用跳过用户态缓冲区，数据从 PageCache 直接到 Socket 缓冲区（2 次 DMA + 1 次 CPU 拷贝），再配合 DMA Scatter-Gather 可以做到 0 次 CPU 拷贝。Java 的 `FileChannel.transferTo()` 底层就是 `sendfile`，Kafka/Nginx 都依赖它实现高吞吐数据传输。

---

## 六、IO 模型 & Epoll（速览）

> ⚡ **详细内容已整合至 [IO模型详解](../IO与网络框架/IO模型详解.md)**，包含五种 IO 模型、BIO/NIO/AIO 代码详解、select/poll/epoll 深度对比、Reactor 模式全解，本节仅保留速查摘要。

### 1. 五种 IO 模型总览

| 模型 | 等待数据 | 复制数据 | 特点 |
|---|---|---|---|
| **BIO（阻塞IO）** | 阻塞 | 阻塞 | 简单，一请求一线程，并发差 |
| **NIO（非阻塞IO）** | 不阻塞（轮询）| 阻塞 | 轮询浪费 CPU |
| **IO 多路复用** | select/epoll 阻塞 | 阻塞 | **一个线程监控多个 fd，Reactor 模式** |
| **信号驱动IO** | 不阻塞（信号）| 阻塞 | 少用 |
| **AIO（异步IO）** | 不阻塞 | 不阻塞 | 全异步，数据复制完才通知 |

### 2. epoll 核心要点

| | select | poll | epoll |
|---|---|---|---|
| **监控 fd 数量** | 1024（FD_SETSIZE）| 无限制 | 无限制 |
| **时间复杂度** | O(n) 每次遍历全量 | O(n) | O(1) 就绪事件 |
| **底层实现** | 位图 | 数组 | **红黑树 + 就绪链表** |
| **适合场景** | fd 少 | fd 少 | **大量连接，少量活跃（C10K）** |

- **ET（边缘触发）**：仅在状态变化时通知一次，必须一次读完数据，性能更高
- **LT（水平触发）**：只要缓冲区有数据就持续通知，编程更安全（JDK NIO 默认）

### 3. 面试标准答法

> Java IO 模型经历了三个阶段。**BIO**（`java.io`）同步阻塞，一连接一线程；**NIO**（`java.nio + Selector`）IO 多路复用，底层调用 `epoll`，一个线程管多个 Channel；**AIO** 真异步但 Linux 不成熟。生产首选 NIO + 主从 Reactor（Netty）。epoll 三大优势：红黑树注册一次不重复拷贝、就绪链表回调 O(1)、无 fd 上限。

> 👉 **深入学习**：[IO模型详解 — BIO/NIO/AIO 全解](../IO与网络框架/IO模型详解.md) | [Reactor 模式](../IO与网络框架/IO模型详解.md#八reactor-模式-)

---

## 七、Linux 常用排查命令

### 1. CPU 问题排查

```bash
# 查看 CPU 使用率 top 进程
top -c                         # 实时监控（按 P 排序 CPU，按 M 排序内存）
top -H -p <pid>               # 查看指定进程的线程 CPU 使用率

# 找到 CPU 最高的线程
top -H -p <pid>               # 找到 CPU 占用高的线程 TID（十进制）
printf '%x\n' <TID>           # 转十六进制，如 12345 → 0x3039
jstack <pid> | grep '0x3039'  # 在堆栈中找对应线程，定位死循环/热点代码

# CPU 整体状态
vmstat 1 5                     # 每1秒打印5次（us:用户态、sy:系统态、id:空闲、wa:IO等待）
mpstat -P ALL 1               # 每核 CPU 使用率
```

**CPU 飙高排查步骤：**
```
1. top → 找 CPU 占用最高的 PID
2. top -H -p PID → 找占用最高的 TID
3. printf '%x' TID → 转16进制
4. jstack PID | grep 16进制 → 找到线程堆栈
5. 分析堆栈 → 死循环/GC 频繁/锁竞争
```

### 2. 内存问题排查

```bash
# 内存总览
free -h                        # 内存使用情况（-h 人类可读单位）
cat /proc/meminfo             # 详细内存信息

# 进程内存
jmap -heap <pid>              # JVM 堆内存使用详情
jmap -histo:live <pid>        # 对象实例统计（top 内存占用类）
jmap -dump:format=b,file=heap.hprof <pid>  # 导出堆快照
# 然后用 MAT(Eclipse Memory Analyzer) 或 JProfiler 分析

# OOM 后自动 dump
# JVM 参数：-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/tmp/

# 查看 GC 情况
jstat -gcutil <pid> 1000      # 每1s打印GC统计（S0/S1/E/O/M各区占比 + YGC/FGC次数）
```

### 3. 网络问题排查

```bash
# 查看连接状态
ss -antup                     # 所有 TCP 连接（-a所有 -n数字 -t TCP -u UDP -p进程）
ss -s                         # 连接统计摘要（ESTABLISHED/TIME_WAIT/CLOSE_WAIT 数量）
netstat -tnlp                 # 监听中的端口

# 查看 CLOSE_WAIT 过多
ss -ant | grep CLOSE_WAIT | wc -l   # CLOSE_WAIT数量（多则服务端未关连接）
ss -ant | grep TIME_WAIT | wc -l    # TIME_WAIT数量（多则主动关闭方，正常现象）

# 抓包（排查请求/响应内容）  
tcpdump -i eth0 port 8080 -w /tmp/cap.pcap     # 抓包保存文件
tcpdump -i eth0 port 8080 -nn -X               # 实时打印（含16进制数据）

# 网络连通性
ping -c 5 <host>              # ICMP 连通性
telnet <host> <port>          # TCP 端口连通性
curl -v http://host/api       # HTTP 请求详情（含响应时间）
curl -o /dev/null -s -w "连接:%{time_connect}s 总:%{time_total}s\n" http://host/api
```

### 4. 磁盘 IO 问题排查

```bash
# 磁盘使用情况
df -h                          # 分区使用情况
du -sh /data/*                # 目录大小（找大文件）

# IO 性能
iostat -x 1 5                 # 块设备 IO 统计（%util：设备繁忙度，await：IO 等待时间）
iotop -o                      # 实时查看 IO 最高的进程

# 找大文件
find / -size +1G -type f      # 找超过1G的文件
lsof | grep deleted           # 找已删除但句柄未关闭的文件（磁盘空间没释放）
```

### 5. 进程 & 日志

```bash
# 进程
ps aux | grep java            # 找 Java 进程
ps -ef --sort=-%cpu | head    # 按 CPU 排序
kill -3 <pid>                 # 发送 SIGQUIT，JVM 打印线程堆栈到标准输出（不杀进程）

# 日志监控
tail -f /var/log/app.log                     # 实时查看日志
tail -f app.log | grep -E "ERROR|Exception"  # 过滤错误
grep -n "OutOfMemoryError" app.log           # 搜索 OOM 错误
awk '{print $1}' access.log | sort | uniq -c | sort -rn | head # 统计 IP 访问量

# 系统负载
uptime                        # 系统运行时间 + 1/5/15分钟负载均值
                              # 负载=CPU核数时是满载；持续>核数则过载
```

### 6. JVM 专项排查命令

```bash
# 查看 JVM 进程
jps -lv                       # 列出 JVM 进程（-l 显示完整类名，-v 显示 JVM 参数）

# 线程分析
jstack <pid>                  # 打印所有线程堆栈（分析死锁、CPU 占用高线程）
jstack -l <pid> | grep "deadlock" -A 20   # 找死锁

# GC 分析
jstat -gc <pid> 1000 10      # 10次，每次间隔1s，打印GC详细数据
jstat -gccause <pid>         # 显示最后一次GC的原因

# 运行时参数
jinfo -flags <pid>           # 查看 JVM 参数
jinfo -flag MaxHeapSize <pid>  # 查看指定参数值

# 远程 JMX 监控（配合 JVisualVM / JConsole）
# JVM 启动参数加：-Dcom.sun.management.jmxremote.port=9010
#                 -Dcom.sun.management.jmxremote.authenticate=false
```

### 7. 排查场景速查

| 现象 | 命令 | 关注点 |
|---|---|---|
| CPU 飙高 | `top -H -p PID` + `jstack` | 死循环、GC 频繁、正则回溯 |
| 内存 OOM | `jmap -dump` + MAT 分析 | 大对象、内存泄漏、静态集合 |
| GC 频繁 | `jstat -gcutil` | YGC 频率、FGC 原因 |
| 接口超时 | `ss -ant` + `tcpdump` | 连接数、TIME_WAIT、网络延迟 |
| 磁盘满 | `df -h` + `du -sh` + `lsof` | 大文件、句柄泄漏 |
| 线程死锁 | `jstack PID \| grep deadlock` | 死锁等待链 |
| CLOSE_WAIT 多 | `ss -ant \| grep CLOSE_WAIT` | 服务端未 close() socket |

---

## 📝 面试速查

| 话题 | 核心答案关键词 |
|---|---|
| 网络分层模型 | OSI七层 / TCP/IP四层 / 五层教学模型 / 封装解封装 / 分层解耦 |
| DNS | 递归+迭代查询 / 浏览器→OS→本地DNS→根→顶级→权威 / A/CNAME/MX记录 |
| ARP | 广播请求→单播回复 / IP解析MAC / 同一局域网 / 跨网段走网关 |
| 三次握手 | SYN / SYN+ACK / ACK / 双向信道验证 / SYN Flood+SYN Cookie |
| 四次挥手 | FIN / 半关闭 / TIME_WAIT 2MSL / 为什么四次 |
| TCP 可靠性 | 序列号+ACK / 超时重传 / 快速重传（3dup ACK）/ 滑动窗口 / 拥塞控制4阶段 |
| HTTPS | TLS握手3步 / 对称+非对称 / CA证书链 / TLS1.3前向安全 |
| 零拷贝 | sendfile / PageCache / DMA / FileChannel.transferTo / Kafka应用 |
| IO模型 | BIO阻塞 / NIO多路复用 / epoll / Reactor主从 / Netty → [详见IO模型详解](../IO与网络框架/IO模型详解.md) |
| epoll | 红黑树+就绪链表 / 回调机制 / ET vs LT / O(1) / 无fd限制 → [详见IO模型详解](../IO与网络框架/IO模型详解.md) |
| Linux排查 | top/jstack(CPU) / jmap+MAT(内存) / ss+tcpdump(网络) / iostat(磁盘) |
